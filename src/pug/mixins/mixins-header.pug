
mixin collapsible_li(title, ...items)
  li(class="bold")
    a(class="collapsible-header waves-effect waves-teal")= `${title}`
    div(class="collapsible-body")
      ul
        each item in items
          li
            a(href=`${item}.html`)= `${item}`

//- +collapsible_li("CSS", "color", "grid")


